#version 430

uniform mat4 orthoProjMx;
uniform int channel; //!< which channel to draw

layout(location = 0) in float in_position;
layout(location = 1) in vec4 in_values;

void main() {
    // --------------------------------------------------------------------------------
    //  TODO: Set the position of the line vertices.
    // --------------------------------------------------------------------------------
}
